# Core runtime
python-dotenv==1.0.1
pydantic==1.10.15
httpx==0.27.0
websockets==12.0
anyio==4.4.0
uvloop==0.19.0; sys_platform != "win32"

# LiveKit (Python SDK; if unavailable pin to latest or replace with official when provided)
livekit==1.0.12

# Audio processing (basic VAD & framing)
webrtcvad==2.0.10
numpy==1.26.4
soundfile==0.12.1

# STT / TTS client libs (choose providers; keep optional)
# Deepgram (STT)
deepgram-sdk==3.1.4
# ElevenLabs (TTS)
elevenlabs==1.3.1

# Async speech streaming helpers (optional)
aiofiles>=24.0.0,<25.0.0

# Logging & metrics
orjson==3.10.7
